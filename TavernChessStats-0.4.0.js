// ==UserScript==
// @name         自走棋本局商店统计
// @namespace    http://tampermonkey.net/
// @version      0.4.0
// @description  统计酒馆自走棋本局商店卡牌出现、刷新与购买虎符消耗，并改为监听实际使用的锦囊牌，支持分回合、拖拽缩放面板和结束下载。
// @author       Codex
// @match        *://*.sanguosha.com/*
// @match        https://game.4399iw2.com/yxxsgs/*
// @grant        none
// @run-at       document-idle
// ==/UserScript==

(function () {
    'use strict';

    const LOG_PREFIX = '[自走棋统计]';
    const STORAGE_KEY = 'tavernChessGameStats';
    const PANEL_ID = 'tavern-chess-stats-panel';
    const HOOK_FLAG = '__tavernChessStatsHooked';
    const SELF_INFO_HOOK_FLAG = '__tavernChessStatsSelfInfoHooked';
    const PROXY_SEND_HOOK_FLAG = '__tavernChessStatsProxySendHooked';
    const SCAN_INTERVAL_MS = 1500;
    const PANEL_STATE_KEY = STORAGE_KEY + ':panelState';
    const SPELL_DUPE_WINDOW_MS = 1200;

    // ============ 棋子和锦囊名称映射 ============
    let chessNameMap = {};
    let spellNameMap = {};
	
	// ID Map数据
    function loadMaps() {
        chessNameMap = {"21001011": "曹休", "21001012": "曹休", "21001021": "夏侯惇", "21001022": "夏侯惇", "21001031": "邓艾", "21001032": "邓艾", "21001041": "夏侯渊", "21001042": "夏侯渊", "21001051": "曹轶", "21001052": "曹轶", "21001061": "薛灵芸", "21001062": "薛灵芸", "21001071": "典韦", "21001072": "典韦", "21001081": "郭嘉", "21001082": "郭嘉", "21001091": "许褚", "21001092": "许褚", "21001101": "荀彧", "21001102": "荀彧", "21001111": "甄姬", "21001112": "甄姬", "21001121": "曹仁", "21001122": "曹仁", "21001131": "曹昂", "21001132": "曹昂", "21001141": "戏志才", "21001142": "戏志才", "21001151": "曹金玉", "21001152": "曹金玉", "21001161": "司马懿", "21001162": "司马懿", "21001171": "张郃", "21001172": "张郃", "21001181": "张辽", "21001182": "张辽", "21001191": "田尚衣", "21001192": "田尚衣", "21001201": "钟会", "21001202": "钟会", "21001211": "程昱", "21001212": "程昱", "21001221": "曹丕", "21001222": "曹丕", "21002021": "张星彩", "21002022": "张星彩", "21002031": "关银屏", "21002032": "关银屏", "21002041": "关羽", "21002042": "关羽", "21002051": "糜竺", "21002052": "糜竺", "21002061": "魏延", "21002062": "魏延", "21002071": "刘禅", "21002072": "刘禅", "21002081": "廖化", "21002082": "廖化", "21002091": "周仓", "21002092": "周仓", "21002101": "赵云", "21002102": "赵云", "21002111": "关兴", "21002112": "关兴", "21002121": "徐庶", "21002122": "徐庶", "21002131": "诸葛亮", "21002132": "诸葛亮", "21002141": "黄忠", "21002142": "黄忠", "21002151": "赵襄", "21002152": "赵襄", "21002161": "张飞", "21002162": "张飞", "21002171": "庞统", "21002172": "庞统", "21002181": "黄月英", "21002182": "黄月英", "21002191": "张苞", "21002192": "张苞", "21002201": "刘永", "21002202": "刘永", "21002211": "法正", "21002212": "法正", "21002221": "姜维", "21002222": "姜维", "21002231": "秦宓", "21002232": "秦宓", "21003011": "吕蒙", "21003012": "吕蒙", "21003021": "陈武", "21003022": "陈武", "21003031": "陆逊", "21003032": "陆逊", "21003041": "程普", "21003042": "程普", "21003051": "丁奉", "21003052": "丁奉", "21003061": "凌统", "21003062": "凌统", "21003071": "黄盖", "21003072": "黄盖", "21003081": "葛玄", "21003082": "葛玄", "21003091": "周善", "21003092": "周善", "21003101": "徐盛", "21003102": "徐盛", "21003111": "甘宁", "21003112": "甘宁", "21003121": "太史慈", "21003122": "太史慈", "21003131": "张昭", "21003132": "张昭", "21003141": "大乔", "21003142": "大乔", "21003151": "孙坚", "21003152": "孙坚", "21003161": "鲁肃", "21003162": "鲁肃", "21003171": "孙尚香", "21003172": "孙尚香", "21003191": "孙翎鸾", "21003192": "孙翎鸾", "21003201": "周瑜", "21003202": "周瑜", "21003211": "孙策", "21003212": "孙策", "21003221": "小乔", "21003222": "小乔", "21003231": "韩当", "21003232": "韩当", "21003241": "董袭", "21003242": "董袭", "21004011": "邓茂", "21004012": "邓茂", "21004021": "眭固", "21004022": "眭固", "21004031": "陶升", "21004032": "陶升", "21004041": "程远志", "21004042": "程远志", "21004051": "波才", "21004052": "波才", "21004061": "严政", "21004062": "严政", "21004071": "何曼", "21004072": "何曼", "21004081": "裴元绍", "21004082": "裴元绍", "21004091": "刘辟", "21004092": "刘辟", "21004101": "卜巳", "21004102": "卜巳", "21004111": "张曼成", "21004112": "张曼成", "21004121": "张闿", "21004122": "张闿", "21004131": "张燕", "21004132": "张燕", "21004141": "马元义", "21004142": "马元义", "21004151": "白绕", "21004152": "白绕", "21004161": "于毒", "21004162": "于毒", "21004171": "管亥", "21004172": "管亥", "21004181": "高升", "21004182": "高升", "21004191": "张宝", "21004192": "张宝", "21004201": "张楚", "21004202": "张楚", "21004211": "张宁", "21004212": "张宁", "21004221": "张梁", "21004222": "张梁", "21005011": "卢植", "21005012": "卢植", "21005021": "冯方", "21005022": "冯方", "21005031": "伏完", "21005032": "伏完", "21005041": "董贵人", "21005042": "董贵人", "21005051": "朱儁", "21005052": "朱儁", "21005061": "刘繇", "21005062": "刘繇", "21005071": "王荣", "21005072": "王荣", "21005081": "郭胜", "21005082": "郭胜", "21005091": "皇甫嵩", "21005092": "皇甫嵩", "21005101": "王允", "21005102": "王允", "21005111": "陈登", "21005112": "陈登", "21005121": "杨彪", "21005122": "杨彪", "21005131": "万年公主", "21005132": "万年公主", "21005141": "曹节", "21005142": "曹节", "21005151": "何进", "21005152": "何进", "21005161": "唐姬", "21005162": "唐姬", "21005171": "张让", "21005172": "张让", "21005181": "赵忠", "21005182": "赵忠", "21005191": "刘辩", "21005192": "刘辩", "21005201": "何太后", "21005202": "何太后", "21005211": "刘宏", "21005212": "刘宏", "21005221": "董承", "21005222": "董承", "21006011": "马云騄", "21006012": "马云騄", "21006021": "段煨", "21006022": "段煨", "21006031": "马休", "21006032": "马休", "21006041": "张横", "21006042": "张横", "21006051": "董白", "21006052": "董白", "21006061": "韩遂", "21006062": "韩遂", "21006071": "马伶俐", "21006072": "马伶俐", "21006081": "董翓", "21006082": "董翓", "21006091": "华雄", "21006092": "华雄", "21006101": "董絮", "21006102": "董絮", "21006111": "张济", "21006112": "张济", "21006121": "马超", "21006122": "马超", "21006131": "徐荣", "21006132": "徐荣", "21006141": "杨婉", "21006142": "杨婉", "21006151": "庞德", "21006152": "庞德", "21006161": "李傕", "21006162": "李傕", "21006171": "马岱", "21006172": "马岱", "21006181": "李肃", "21006182": "李肃", "21006191": "马铁", "21006192": "马铁", "21006201": "樊稠", "21006202": "樊稠", "21006211": "马腾", "21006212": "马腾", "21006221": "郭汜", "21006222": "郭汜", "21007011": "颜良", "21007012": "颜良", "21007021": "桥蕤", "21007022": "桥蕤", "21007031": "审配", "21007032": "审配", "21007041": "雷薄", "21007042": "雷薄", "21007051": "高览", "21007052": "高览", "21007061": "冯妤", "21007062": "冯妤", "21007071": "许攸", "21007072": "许攸", "21007081": "纪灵", "21007082": "纪灵", "21007091": "逢纪", "21007092": "逢纪", "21007101": "张勋", "21007102": "张勋", "21007111": "文丑", "21007112": "文丑", "21007121": "杨弘", "21007122": "杨弘", "21007131": "乐就", "21007132": "乐就", "21007141": "袁胤", "21007142": "袁胤", "21007151": "淳于琼", "21007152": "淳于琼", "21007161": "郭图", "21007162": "郭图", "21007171": "韩猛", "21007172": "韩猛", "21007181": "麹义", "21007182": "麹义", "21007191": "董绾", "21007192": "董绾", "21007201": "袁绍", "21007202": "袁绍", "21007211": "袁术", "21007212": "袁术", "21007221": "田丰", "21007222": "田丰", "21007231": "沮授", "21007232": "沮授", "21008011": "陶谦", "21008012": "陶谦", "21008021": "邹氏", "21008022": "邹氏", "21008031": "蔡夫人", "21008032": "蔡夫人", "21008041": "蔡邕", "21008042": "蔡邕", "21008051": "严夫人", "21008052": "严夫人", "21008061": "士燮", "21008062": "士燮", "21008071": "吉平", "21008072": "吉平", "21008081": "陈珪", "21008082": "陈珪", "21008091": "貂蝉", "21008092": "貂蝉", "21008101": "潘凤", "21008102": "潘凤", "21008111": "司马徽", "21008112": "司马徽", "21008121": "童渊", "21008122": "童渊", "21008131": "邢道荣", "21008132": "邢道荣", "21008141": "张鲁", "21008142": "张鲁", "21008151": "胡车儿", "21008152": "胡车儿", "21008161": "管宁", "21008162": "管宁", "21008171": "韩馥", "21008172": "韩馥", "21008181": "华佗", "21008182": "华佗", "21008191": "祢衡", "21008192": "祢衡", "21008201": "刘表", "21008202": "刘表", "21008211": "于吉", "21008212": "于吉", "21008221": "庞德公", "21008222": "庞德公", "21008231": "黄承彦", "21008232": "黄承彦", "21008241": "来莺儿", "21008242": "来莺儿", "21008251": "贾诩", "21008252": "贾诩", "21008261": "吕布", "21008262": "吕布", "21008271": "南华老仙", "21008272": "南华老仙", "21008281": "许劭", "21008282": "许劭", "21008291": "左慈", "21008292": "左慈", "21008301": "蔡文姬", "21008302": "蔡文姬"};
		spellNameMap = {"2024210001": "珠联璧合·二", "2024210021": "珠联璧合·三", "2024210022": "珠联璧合·四", "2024210023": "珠联璧合·五", "2024210024": "珠联璧合·六", "2024210025": "洞烛先机·二", "2024210026": "兵贵神速·二", "2024210027": "酒·二", "2024210028": "桃·二", "2024210029": "金蝉脱壳·二", "2024210030": "联军盛宴·二", "2024210031": "万箭齐发·二", "2024210032": "南蛮入侵·二", "2024210033": "无中生有·三", "2024210034": "顺手牵羊·三", "2024210035": "随机应变·三", "2024210036": "洞烛先机·三", "2024210037": "兵贵神速·三", "2024210038": "酒·三", "2024210039": "桃·三", "2024210040": "联军盛宴·三", "2024210041": "桃园结义·三", "2024210042": "天降神兵·三", "2024210043": "五谷丰登·三", "2024210044": "铁索连环·三", "2024210045": "斗转星移·四", "2024210046": "白衣渡江·三", "2024210047": "闪电·三", "2024210048": "苦肉计·三", "2024210049": "无中生有·四", "2024210050": "随机应变·四", "2024210051": "洞烛先机·四", "2024210052": "兵贵神速·四", "2024210053": "酒·四", "2024210054": "桃·四", "2024210055": "联军盛宴·四", "2024210056": "万箭齐发·四", "2024210057": "南蛮入侵·四", "2024210058": "桃园结义·四", "2024210059": "天降神兵·四", "2024210060": "五谷丰登·四", "2024210061": "铁索连环·四", "2024210063": "决斗·四", "2024210064": "以逸待劳·四", "2024210065": "望梅止渴·四", "2024210066": "乐不思蜀·四", "2024210067": "抛砖引玉·三", "2024210068": "偷梁换柱·四", "2024210069": "无中生有·五", "2024210070": "随机应变·五", "2024210071": "洞烛先机·五", "2024210072": "兵贵神速·五", "2024210073": "酒·五", "2024210074": "桃·五", "2024210075": "联军盛宴·五", "2024210076": "万箭齐发·五", "2024210077": "南蛮入侵·五", "2024210078": "桃园结义·五", "2024210079": "天降神兵·五", "2024210080": "五谷丰登·五", "2024210081": "铁索连环·五", "2024210083": "白衣渡江·五", "2024210084": "闪电·五", "2024210085": "苦肉计·五", "2024210086": "决斗·五", "2024210087": "以逸待劳·五", "2024210088": "兵粮寸断·五", "2024210089": "草船借箭·五", "2024210090": "瞒天过海·五", "2024210091": "水淹七军·五", "2024210092": "无中生有·六", "2024210093": "随机应变·六", "2024210094": "万箭齐发·六", "2024210095": "南蛮入侵·六", "2024210096": "桃园结义·六", "2024210097": "天降神兵·六", "2024210098": "五谷丰登·六", "2024210100": "决斗·六", "2024210101": "以逸待劳·六", "2024210102": "望梅止渴·六", "2024210103": "乐不思蜀·六", "2024210104": "李代桃僵·六", "2024210105": "无懈可击·六", "2024210107": "浑水摸鱼·六", "30021": "鸩毒·六", "30031": "火攻·四", "30032": "火攻·五", "30033": "火攻·六", "302011": "珠联璧合·二", "302012": "珠联璧合·三", "302013": "珠联璧合·四", "302014": "珠联璧合·五", "302015": "珠联璧合·六", "302021": "洞烛先机·二", "302022": "洞烛先机·三", "302023": "洞烛先机·四", "302024": "洞烛先机·五", "302031": "兵贵神速·二", "302032": "兵贵神速·三", "302033": "兵贵神速·四", "302034": "兵贵神速·五", "302041": "酒·二", "302042": "酒·三", "302043": "酒·四", "302044": "酒·五", "302051": "桃·二", "302052": "桃·三", "302053": "桃·四", "302054": "桃·五", "302061": "金蝉脱壳·二", "302071": "联军盛宴·二", "302072": "联军盛宴·三", "302073": "联军盛宴·四", "302074": "联军盛宴·五", "302081": "万箭齐发·二", "302082": "万箭齐发·四", "302083": "万箭齐发·五", "302084": "万箭齐发·六", "302091": "南蛮入侵·二", "302092": "南蛮入侵·四", "302093": "南蛮入侵·五", "302094": "南蛮入侵·六", "302101": "无中生有·三", "302102": "无中生有·四", "302103": "无中生有·五", "302104": "无中生有·六", "302111": "顺手牵羊·三", "302121": "随机应变·三", "302122": "随机应变·四", "302123": "随机应变·五", "302124": "随机应变·六", "302131": "桃园结义·三", "302132": "桃园结义·四", "302133": "桃园结义·五", "302134": "桃园结义·六", "302141": "天降神兵·三", "302142": "天降神兵·四", "302143": "天降神兵·五", "302144": "天降神兵·六", "302151": "五谷丰登·三", "302152": "五谷丰登·四", "302153": "五谷丰登·五", "302154": "五谷丰登·六", "302161": "铁索连环·三", "302162": "铁索连环·四", "302163": "铁索连环·五", "302171": "白衣渡江·三", "302172": "白衣渡江·五", "302181": "闪电·三", "302182": "闪电·五", "302191": "苦肉计·三", "302192": "苦肉计·五", "302201": "抛砖引玉·三", "302211": "斗转星移·四", "302221": "决斗·四", "302222": "决斗·五", "302223": "决斗·六", "302231": "以逸待劳·四", "302232": "以逸待劳·五", "302233": "以逸待劳·六", "302241": "望梅止渴·四", "302242": "望梅止渴·六", "302251": "乐不思蜀·四", "302252": "乐不思蜀·六", "302261": "瞒天过海·五", "302271": "水淹七军·五", "302281": "火攻·四", "302282": "火攻·五", "302283": "火攻·六", "302291": "李代桃僵·六", "302301": "无懈可击·六", "302311": "浑水摸鱼·六", "302321": "鸩毒·六", "302331": "草船借箭·五", "303011": "珠联璧合·二", "303012": "珠联璧合·三", "303013": "珠联璧合·四", "303014": "珠联璧合·五", "303015": "珠联璧合·六", "303021": "酒·二", "303022": "酒·四", "303023": "酒·六", "303031": "桃·二", "303032": "桃·四", "303033": "桃·六", "303041": "万箭齐发·二", "303042": "万箭齐发·四", "303043": "万箭齐发·六", "303051": "洞烛先机·二", "303052": "洞烛先机·四", "303061": "金蝉脱壳·二", "303062": "金蝉脱壳·四", "303071": "先驱·二", "303072": "先驱·四", "303081": "调虎离山·三", "303091": "过河拆桥·三", "303092": "过河拆桥·五", "303101": "苦肉计·三", "303102": "苦肉计·五", "303111": "乐不思蜀·三", "303121": "抛砖引玉·三", "303131": "顺手牵羊·三", "303132": "顺手牵羊·五", "303141": "偷梁换柱·三", "303142": "偷梁换柱·五", "303151": "望梅止渴·三", "303152": "望梅止渴·五", "303161": "无中生有·三", "303162": "无中生有·五", "303171": "以逸待劳·三", "303172": "以逸待劳·五", "303181": "斗转星移·四", "303191": "火攻·四", "303192": "火攻·六", "303201": "解甲归田·四", "303211": "决斗·四", "303212": "决斗·六", "303221": "南蛮入侵·四", "303231": "三顾茅庐·四", "303241": "闪电·四", "303251": "桃园结义·四", "303261": "五谷丰登·四", "303262": "五谷丰登·六", "303271": "草船借箭·五", "303281": "联军盛宴·五", "303291": "美人计·五", "303301": "撒豆成兵·五", "303311": "三十六计·五", "303321": "树上开花·五", "303331": "远交近攻·五", "303341": "兵贵神速·六", "303351": "釜底抽薪·六", "303361": "关门捉贼·六", "303371": "浑水摸鱼·六", "303381": "借刀杀人·六", "303391": "借尸还魂·六", "303401": "瞒天过海·六", "303411": "擒贼擒王·六", "303421": "随机应变·六", "303431": "天降神兵·六", "303441": "无懈可击·六", "303451": "鸩毒·六", "304011": "珠联璧合·二", "304012": "珠联璧合·三", "304013": "珠联璧合·四", "304014": "珠联璧合·五", "304015": "珠联璧合·六", "304021": "酒·二", "304022": "酒·四", "304023": "酒·六", "304031": "桃·二", "304032": "桃·四", "304033": "桃·六", "304041": "万箭齐发·二", "304042": "万箭齐发·四", "304043": "万箭齐发·六", "304051": "洞烛先机·二", "304052": "洞烛先机·四", "304061": "金蝉脱壳·二", "304062": "金蝉脱壳·四", "304071": "先驱·二", "304072": "先驱·四", "304081": "调虎离山·三", "304091": "过河拆桥·三", "304092": "过河拆桥·五", "304101": "苦肉计·三", "304102": "苦肉计·五", "304111": "乐不思蜀·三", "304121": "抛砖引玉·三", "304131": "顺手牵羊·三", "304132": "顺手牵羊·五", "304141": "偷梁换柱·三", "304142": "偷梁换柱·五", "304151": "望梅止渴·三", "304152": "望梅止渴·五", "304161": "无中生有·三", "304162": "无中生有·五", "304171": "以逸待劳·三", "304172": "以逸待劳·五", "304181": "斗转星移·四", "304191": "火攻·四", "304192": "火攻·六", "304201": "解甲归田·四", "304211": "决斗·四", "304212": "决斗·六", "304221": "南蛮入侵·四", "304231": "三顾茅庐·三", "304241": "闪电·四", "304251": "桃园结义·四", "304261": "五谷丰登·四", "304262": "五谷丰登·六", "304271": "草船借箭·五", "304281": "联军盛宴·五", "304291": "美人计·五", "304301": "撒豆成兵·五", "304311": "三十六计·五", "304321": "树上开花·五", "304331": "远交近攻·五", "304341": "兵贵神速·六", "304351": "釜底抽薪·六", "304361": "关门捉贼·六", "304371": "浑水摸鱼·六", "304381": "借刀杀人·六", "304391": "借尸还魂·六", "304401": "瞒天过海·六", "304411": "擒贼擒王·六", "304421": "随机应变·六", "304431": "天降神兵·六", "304441": "无懈可击·六", "304451": "鸩毒·六", "305011": "珠联璧合·二", "305012": "珠联璧合·三", "305013": "珠联璧合·四", "305014": "珠联璧合·五", "305015": "珠联璧合·六", "305021": "酒·二", "305022": "酒·四", "305023": "酒·六", "305031": "桃·二", "305032": "桃·四", "305033": "桃·六", "305041": "万箭齐发·二", "305042": "万箭齐发·四", "305043": "万箭齐发·六", "305051": "洞烛先机·二", "305052": "洞烛先机·四", "305061": "金蝉脱壳·二", "305062": "金蝉脱壳·四", "305071": "先驱·二", "305072": "先驱·四", "305081": "调虎离山·三", "305091": "过河拆桥·三", "305092": "过河拆桥·五", "305101": "苦肉计·三", "305102": "苦肉计·五", "305111": "乐不思蜀·三", "305121": "抛砖引玉·三", "305131": "顺手牵羊·三", "305132": "顺手牵羊·五", "305141": "偷梁换柱·三", "305142": "偷梁换柱·五", "305151": "望梅止渴·三", "305152": "望梅止渴·五", "305161": "无中生有·三", "305162": "无中生有·五", "305171": "以逸待劳·三", "305172": "以逸待劳·五", "305181": "斗转星移·四", "305191": "火攻·四", "305192": "火攻·六", "305201": "解甲归田·四", "305211": "决斗·三", "305212": "决斗·五", "305221": "南蛮入侵·四", "305231": "三顾茅庐·三", "305241": "闪电·四", "305251": "桃园结义·四", "305261": "五谷丰登·四", "305262": "五谷丰登·六", "305271": "草船借箭·五", "305281": "联军盛宴·五", "305291": "美人计·五", "305301": "撒豆成兵·五", "305311": "三十六计·五", "305321": "树上开花·五", "305331": "远交近攻·五", "305341": "兵贵神速·六", "305351": "釜底抽薪·六", "305361": "关门捉贼·六", "305371": "浑水摸鱼·六", "305381": "借刀杀人·六", "305391": "借尸还魂·六", "305401": "瞒天过海·六", "305411": "擒贼擒王·六", "305421": "随机应变·六", "305431": "天降神兵·六", "305441": "无懈可击·六", "305451": "鸩毒·六", "306001": "兵贵神速·六", "306002": "草船借箭·五", "306003": "调虎离山·四", "306004": "洞烛先机·二", "306005": "洞烛先机·三", "306006": "洞烛先机·四", "306007": "斗转星移·四", "306008": "浮雷·四", "306009": "釜底抽薪·六", "306010": "关门捉贼·六", "306011": "过河拆桥·三", "306012": "过河拆桥·五", "306013": "浑水摸鱼·六", "306014": "火攻·四", "306015": "火攻·六", "306016": "火上浇油·三", "306017": "火上浇油·五", "306018": "借刀杀人·六", "306019": "借尸还魂·六", "306020": "金蝉脱壳·二", "306021": "金蝉脱壳·四", "306022": "酒·二", "306023": "酒·三", "306024": "酒·四", "306025": "酒·五", "306026": "酒·六", "306027": "决斗·三", "306028": "决斗·五", "306029": "苦肉计·三", "306030": "苦肉计·五", "306031": "乐不思蜀·四", "306032": "联军盛宴·六", "306033": "瞒天过海·六", "306034": "美人计·五", "306035": "南蛮入侵·二", "306036": "南蛮入侵·四", "306037": "抛砖引玉·四", "306038": "破釜沉舟·六", "306039": "破阵溃敌·三", "306040": "破阵溃敌·五", "306041": "齐心并力·三", "306042": "齐心并力·四", "306043": "齐心并力·五", "306044": "擒贼擒王·六", "306045": "撒豆成兵·五", "306046": "三顾茅庐·三", "306047": "三十六计·五", "306048": "闪电·五", "306049": "树上开花·五", "306050": "顺手牵羊·三", "306051": "顺手牵羊·五", "306052": "随机应变·六", "306053": "桃·二", "306054": "桃·三", "306055": "桃·四", "306056": "桃·五", "306057": "桃·六", "306058": "桃园结义·三", "306059": "桃园结义·四", "306060": "桃园结义·五", "306061": "天降神兵·五", "306062": "偷梁换柱·四", "306063": "偷梁换柱·六", "306064": "屯粮·二", "306065": "屯粮·四", "306066": "万箭齐发·六", "306067": "望梅止渴·六", "306068": "无懈可击·六", "306069": "无中生有·三", "306070": "无中生有·五", "306071": "五谷丰登·六", "306072": "先驱·二", "306073": "先驱·三", "306074": "先驱·四", "306075": "一马当先·四", "306076": "以逸待劳·五", "306077": "援军·三", "306078": "援军·五", "306079": "远交近攻·五", "306080": "增兵减灶·四", "306081": "鸩毒·六", "306082": "至死方休·六", "306083": "珠联璧合·二", "306084": "珠联璧合·三", "306085": "珠联璧合·四", "306086": "珠联璧合·五", "306087": "珠联璧合·六", "306088": "风调雨顺·六", "306089": "云行雨施·六", "306090": "海晏河清·六", "306091": "苦肉计·四", "307001": "兵贵神速·六", "307002": "草船借箭·五", "307003": "调虎离山·四", "307004": "洞烛先机·二", "307005": "洞烛先机·三", "307006": "洞烛先机·四", "307007": "斗转星移·四", "307008": "浮雷·四", "307009": "釜底抽薪·六", "307010": "关门捉贼·六", "307011": "过河拆桥·三", "307012": "过河拆桥·五", "307013": "浑水摸鱼·六", "307014": "火攻·四", "307015": "火攻·六", "307016": "火上浇油·三", "307017": "火上浇油·五", "307018": "借刀杀人·六", "307019": "借尸还魂·六", "307020": "金蝉脱壳·二", "307021": "金蝉脱壳·四", "307022": "酒·二", "307023": "酒·三", "307024": "酒·四", "307025": "酒·五", "307026": "酒·六", "307027": "决斗·三", "307028": "决斗·五", "307029": "苦肉计·三", "307030": "苦肉计·五", "307031": "乐不思蜀·四", "307032": "联军盛宴·六", "307033": "瞒天过海·六", "307034": "美人计·五", "307035": "南蛮入侵·二", "307036": "南蛮入侵·四", "307037": "抛砖引玉·四", "307038": "破釜沉舟·六", "307039": "破阵溃敌·三", "307040": "破阵溃敌·五", "307041": "齐心并力·三", "307042": "齐心并力·四", "307043": "齐心并力·五", "307044": "擒贼擒王·六", "307045": "撒豆成兵·五", "307046": "三顾茅庐·三", "307047": "三十六计·五", "307048": "闪电·五", "307049": "树上开花·五", "307050": "顺手牵羊·三", "307051": "顺手牵羊·五", "307052": "随机应变·六", "307053": "桃·二", "307054": "桃·三", "307055": "桃·四", "307056": "桃·五", "307057": "桃·六", "307058": "桃园结义·三", "307059": "桃园结义·四", "307060": "桃园结义·五", "307061": "天降神兵·五", "307062": "偷梁换柱·四", "307063": "偷梁换柱·六", "307064": "屯粮·二", "307065": "屯粮·四", "307066": "万箭齐发·六", "307067": "望梅止渴·六", "307068": "无懈可击·六", "307069": "无中生有·三", "307070": "无中生有·五", "307071": "五谷丰登·六", "307072": "先驱·二", "307073": "先驱·三", "307074": "先驱·四", "307075": "一马当先·四", "307076": "以逸待劳·五", "307077": "援军·三", "307078": "援军·五", "307079": "远交近攻·五", "307080": "增兵减灶·四", "307081": "鸩毒·六", "307082": "至死方休·六", "307083": "珠联璧合·二", "307084": "珠联璧合·三", "307085": "珠联璧合·四", "307086": "珠联璧合·五", "307087": "珠联璧合·六", "307088": "风调雨顺·六", "307089": "云行雨施·六", "307090": "海晏河清·六", "307091": "苦肉计·四", "308001": "兵贵神速·六", "308002": "草船借箭·五", "308003": "调虎离山·四", "308004": "洞烛先机·二", "308005": "洞烛先机·三", "308006": "洞烛先机·四", "308007": "斗转星移·四", "308008": "浮雷·四", "308009": "釜底抽薪·六", "308010": "关门捉贼·六", "308011": "过河拆桥·三", "308012": "过河拆桥·五", "308013": "浑水摸鱼·六", "308014": "火攻·四", "308015": "火攻·六", "308016": "火上浇油·三", "308017": "火上浇油·五", "308018": "借刀杀人·六", "308019": "借尸还魂·六", "308020": "金蝉脱壳·二", "308021": "金蝉脱壳·四", "308022": "酒·二", "308023": "酒·三", "308024": "酒·四", "308025": "酒·五", "308026": "酒·六", "308027": "决斗·三", "308028": "决斗·五", "308029": "苦肉计·三", "308030": "苦肉计·五", "308031": "乐不思蜀·四", "308032": "联军盛宴·六", "308033": "瞒天过海·六", "308034": "美人计·五", "308035": "南蛮入侵·二", "308036": "南蛮入侵·四", "308037": "抛砖引玉·四", "308038": "破釜沉舟·六", "308039": "破阵溃敌·三", "308040": "破阵溃敌·五", "308041": "齐心并力·三", "308042": "齐心并力·四", "308043": "齐心并力·五", "308044": "擒贼擒王·六", "308045": "撒豆成兵·五", "308046": "三顾茅庐·三", "308047": "三十六计·五", "308048": "闪电·五", "308049": "树上开花·五", "308050": "顺手牵羊·三", "308051": "顺手牵羊·五", "308052": "随机应变·六", "308053": "桃·二", "308054": "桃·三", "308055": "桃·四", "308056": "桃·五", "308057": "桃·六", "308058": "桃园结义·三", "308059": "桃园结义·四", "308060": "桃园结义·五", "308061": "天降神兵·五", "308062": "偷梁换柱·四", "308063": "偷梁换柱·六", "308064": "屯粮·二", "308065": "屯粮·四", "308066": "万箭齐发·六", "308067": "望梅止渴·六", "308068": "无懈可击·六", "308069": "无中生有·三", "308070": "无中生有·五", "308071": "五谷丰登·六", "308072": "先驱·二", "308073": "先驱·三", "308074": "先驱·四", "308075": "一马当先·四", "308076": "以逸待劳·五", "308077": "援军·三", "308078": "援军·五", "308079": "远交近攻·五", "308080": "增兵减灶·四", "308081": "鸩毒·六", "308082": "至死方休·六", "308083": "珠联璧合·二", "308084": "珠联璧合·三", "308085": "珠联璧合·四", "308086": "珠联璧合·五", "308087": "珠联璧合·六", "308088": "风调雨顺·六", "308089": "云行雨施·六", "308090": "海晏河清·六", "308091": "苦肉计·四", "309001": "兵贵神速·六", "309002": "草船借箭·五", "309003": "调虎离山·四", "309004": "洞烛先机·二", "309005": "洞烛先机·三", "309006": "洞烛先机·四", "309007": "斗转星移·四", "309008": "浮雷·四", "309009": "釜底抽薪·六", "309010": "关门捉贼·六", "309011": "过河拆桥·三", "309012": "过河拆桥·五", "309013": "浑水摸鱼·六", "309014": "火攻·四", "309015": "火攻·六", "309016": "火上浇油·三", "309017": "火上浇油·五", "309018": "借刀杀人·六", "309019": "借尸还魂·六", "309020": "金蝉脱壳·二", "309021": "金蝉脱壳·四", "309022": "酒·二", "309023": "酒·三", "309024": "酒·四", "309025": "酒·五", "309026": "酒·六", "309027": "决斗·三", "309028": "决斗·五", "309029": "苦肉计·三", "309030": "苦肉计·五", "309031": "乐不思蜀·四", "309032": "联军盛宴·六", "309033": "瞒天过海·六", "309034": "美人计·五", "309035": "南蛮入侵·二", "309036": "南蛮入侵·四", "309037": "抛砖引玉·四", "309038": "破釜沉舟·六", "309039": "破阵溃敌·三", "309040": "破阵溃敌·五", "309041": "齐心并力·三", "309042": "齐心并力·四", "309043": "齐心并力·五", "309044": "擒贼擒王·六", "309045": "撒豆成兵·五", "309046": "三顾茅庐·三", "309047": "三十六计·五", "309048": "闪电·五", "309049": "树上开花·五", "309050": "顺手牵羊·三", "309051": "顺手牵羊·五", "309052": "随机应变·六", "309053": "桃·二", "309054": "桃·三", "309055": "桃·四", "309056": "桃·五", "309057": "桃·六", "309058": "桃园结义·三", "309059": "桃园结义·四", "309060": "桃园结义·五", "309061": "天降神兵·五", "309062": "偷梁换柱·四", "309063": "偷梁换柱·六", "309064": "屯粮·二", "309065": "屯粮·四", "309066": "万箭齐发·六", "309067": "望梅止渴·六", "309068": "无懈可击·六", "309069": "无中生有·三", "309070": "无中生有·五", "309071": "五谷丰登·六", "309072": "先驱·二", "309073": "先驱·三", "309074": "先驱·四", "309075": "一马当先·四", "309076": "以逸待劳·五", "309077": "援军·三", "309078": "援军·五", "309079": "远交近攻·五", "309080": "增兵减灶·四", "309081": "鸩毒·六", "309082": "至死方休·六", "309083": "珠联璧合·二", "309084": "珠联璧合·三", "309085": "珠联璧合·四", "309086": "珠联璧合·五", "309087": "珠联璧合·六", "309088": "风调雨顺·六", "309089": "云行雨施·六", "309090": "海晏河清·六", "309091": "苦肉计·四", "309093": "投石问路·六", "309094": "兵粮寸断·六", "310001": "兵贵神速·六", "310002": "草船借箭·五", "310003": "调虎离山·四", "310004": "洞烛先机·二", "310005": "洞烛先机·三", "310006": "洞烛先机·四", "310007": "斗转星移·四", "310008": "浮雷·四", "310009": "釜底抽薪·六", "310010": "关门捉贼·六", "310011": "过河拆桥·三", "310012": "过河拆桥·五", "310013": "浑水摸鱼·六", "310014": "火攻·四", "310015": "火攻·六", "310016": "火上浇油·三", "310017": "火上浇油·五", "310018": "借刀杀人·六", "310019": "借尸还魂·六", "310020": "金蝉脱壳·二", "310021": "金蝉脱壳·四", "310022": "酒·二", "310023": "酒·三", "310024": "酒·四", "310025": "酒·五", "310026": "酒·六", "310027": "决斗·三", "310028": "决斗·五", "310029": "苦肉计·三", "310030": "苦肉计·五", "310031": "乐不思蜀·四", "310032": "联军盛宴·六", "310033": "瞒天过海·六", "310034": "美人计·五", "310035": "南蛮入侵·二", "310036": "南蛮入侵·四", "310037": "抛砖引玉·四", "310038": "破釜沉舟·六", "310039": "破阵溃敌·三", "310040": "破阵溃敌·五", "310041": "齐心并力·三", "310042": "齐心并力·四", "310043": "齐心并力·五", "310044": "擒贼擒王·六", "310045": "撒豆成兵·五", "310046": "三顾茅庐·三", "310047": "三十六计·五", "310048": "闪电·五", "310049": "树上开花·五", "310050": "顺手牵羊·三", "310051": "顺手牵羊·五", "310052": "随机应变·六", "310053": "桃·二", "310054": "桃·三", "310055": "桃·四", "310056": "桃·五", "310057": "桃·六", "310058": "桃园结义·三", "310059": "桃园结义·四", "310060": "桃园结义·五", "310061": "天降神兵·五", "310062": "偷梁换柱·四", "310063": "偷梁换柱·六", "310064": "屯粮·二", "310065": "屯粮·四", "310066": "万箭齐发·六", "310067": "望梅止渴·六", "310068": "无懈可击·六", "310069": "无中生有·三", "310070": "无中生有·五", "310071": "五谷丰登·六", "310072": "先驱·二", "310073": "先驱·三", "310074": "先驱·四", "310075": "一马当先·四", "310076": "以逸待劳·五", "310077": "援军·三", "310078": "援军·五", "310079": "远交近攻·五", "310080": "增兵减灶·四", "310081": "鸩毒·六", "310082": "至死方休·六", "310083": "珠联璧合·二", "310084": "珠联璧合·三", "310085": "珠联璧合·四", "310086": "珠联璧合·五", "310087": "珠联璧合·六", "310088": "风调雨顺·六", "310089": "云行雨施·六", "310090": "海晏河清·六", "310091": "苦肉计·四"};
    }
    function getChessName(chessId) {
        if (!chessId) return '未知棋子';
        const idStr = String(chessId);
        if (chessNameMap[idStr]) return chessNameMap[idStr];
        const last3 = idStr.slice(-3);
        if (chessNameMap[last3]) return chessNameMap[last3];
        const last4 = idStr.slice(-4);
        if (chessNameMap[last4]) return chessNameMap[last4];
        return idStr;
    }

    function getSpellName(spellId) {
        if (!spellId) return '未知锦囊';
        const idStr = String(spellId);
        return spellNameMap[idStr] || idStr;
    }

    let currentScene = null;
    let currentManager = null;
    let currentStats = null;
    let panelBody = null;
    let panelStatus = null;
    let lastShopSignature = '';
    let downloadedGameIds = new Set();
    let recordedSpellInstanceKeys = new Map();
    let isMinimized = false;
    let panelContent = null;
    let pendingSpellUses = [];
    let latestBuyCard = null;
    let latestLineupCard = null;
    let latestSpellUseCard = null;

    function findTavernChessScene(root, visited) {
        if (!root || typeof root !== 'object') return null;
        const seen = visited || new WeakSet();
        if (seen.has(root)) return null;
        seen.add(root);

        if (root.constructor && root.constructor.name === 'TavernChessGameScene') return root;

        const children = root._children || root.children || root.childList;
        if (Array.isArray(children)) {
            for (const child of children) {
                const result = findTavernChessScene(child, seen);
                if (result) return result;
            }
        }

        const childCount = Number(root.numChildren || 0);
        if (childCount > 0 && typeof root.getChildAt === 'function') {
            for (let i = 0; i < childCount; i++) {
                const result = findTavernChessScene(root.getChildAt(i), seen);
                if (result) return result;
            }
        }
        return null;
    }

    function waitAndAttach() {
        loadMaps();
        createPanel();
        setInterval(function () {
            try {
                if (!window.Laya || !Laya.stage) {
                    updateStatus('等待 Laya.stage ...');
                    return;
                }

                const scene = findTavernChessScene(Laya.stage);
                const manager = scene && scene.manager;
                if (!scene || !manager) {
                    updateStatus(currentStats ? '未找到自走棋场景，保留上一局数据' : '未找到自走棋场景');
                    return;
                }

                if (manager !== currentManager) {
                    currentScene = scene;
                    currentManager = manager;
                    currentStats = createGameStats(manager);
                    lastShopSignature = '';
                    recordedSpellInstanceKeys = new Map();
                    attachManagerHooks(manager);
                    attachSelfInfoHooks(manager);
                    // 初始记录商店（第1回合，保留）
                    recordShopGoods(manager.ShopGoods || manager.shopGoods, { source: 'attach', animate: false });
                    saveStats();
                    renderPanel();
                    console.log(LOG_PREFIX, '已连接 manager', currentStats.gameId);
                } else {
                    attachSelfInfoHooks(manager);
                    renderPanel();
                }
            } catch (error) {
                console.error(LOG_PREFIX, error);
                updateStatus('扫描异常：' + error.message);
            }
        }, SCAN_INTERVAL_MS);
    }

    function createGameStats(manager) {
        const now = new Date();
        const self = manager.selfInfo || manager.SelfInfo || {};
        const user = window.UserData && UserData.Self ? UserData.Self : {};
        const userId = self.userID || self.UserID || user.UserID || user.userID || window.userID || '';
        const nickName = self.nickName || self.nickname || self.NickName || user.NickName || user.nickName || '';
        const tableId = manager.TableID || manager.tableID || manager.tableId || '';

        return {
            version: 2,
            gameId: [tableId || 'unknown', userId || 'user', now.getTime()].join('_'),
            tableId,
            userId,
            nickName,
            startedAt: now.toISOString(),
            updatedAt: now.toISOString(),
            endedAt: '',
            totals: createRoundStats('整局'),
            rounds: {},
            shopAppearances: {},
            spells: {},
            cardStates: {
                hand: [],
                lineup: [],
                shop: []
            },
            snapshots: [],
            events: []
        };
    }

    function createRoundStats(label) {
        return {
            label,
            refresh: { count: 0, hufu: 0, freeCount: 0, autoCount: 0, details: [] },
            buy: { count: 0, hufu: 0, details: [] },
            shop: { snapshots: 0, appearances: {}, details: [] },
            cardStates: {
                hand: [],
                lineup: [],
                shop: []
            }
        };
    }

    function getRoundKey() {
        if (!currentManager) return 'unknown';
        const round = currentManager.CurRound || currentManager.curRound || currentManager.round || currentManager.Turn || currentManager.turn;
        return round || round === 0 ? String(round) : 'unknown';
    }

    function getRoundStats(roundKey) {
        const key = String(roundKey || getRoundKey());
        if (!currentStats.rounds[key]) currentStats.rounds[key] = createRoundStats('第 ' + key + ' 回合');
        return currentStats.rounds[key];
    }

    function buildSpellPayloadFromEvent(manager, spellID, chessID) {
        const normalizedSpellID = Number(spellID) || 0;
        const normalizedChessID = Number(chessID) || 0;
        const name = normalizedSpellID ? getSpellName(normalizedSpellID) : (normalizedChessID ? getChessName(normalizedChessID) : '未知锦囊');
        const payload = {
            goodsID: 0,
            spellID: normalizedSpellID,
            chessID: normalizedChessID,
            name: name,
            raw: { spellID: normalizedSpellID, chessID: normalizedChessID }
        };

        const handCards = Array.isArray(manager && (manager.HandChess || manager.handChess))
            ? (manager.HandChess || manager.handChess)
            : [];
        if (Array.isArray(handCards)) {
            const matched = handCards.find(function (item) {
                if (!item || typeof item !== 'object') return false;
                return (Number(item.spellID || item.SpellID || 0) === normalizedSpellID)
                    || (Number(item.chessID || item.ChessID || item.cardID || item.CardID || 0) === normalizedChessID);
            });
            if (matched) {
                payload.goodsID = matched.goodsID || matched.GoodsID || payload.goodsID;
                payload.raw = matched;
            }
        }
        return payload;
    }

    function attachManagerHooks(manager) {
        if (manager[HOOK_FLAG]) return;
        Object.defineProperty(manager, HOOK_FLAG, { value: true, configurable: true });

        // ===== 记录刷新请求（参考0.1.0版本） =====
        wrapMethod(manager, 'ReqShopRefreshChess', function (original, args) {
            const isAuto = !!args[0];
            const cost = Number(safeCall(this, 'ShopRefreshCost')) || 0;
            const roundKey = getRoundKey();
            // 直接记录刷新
            recordCost('refresh', {
                cost: cost,
                isAuto: isAuto,
                phase: this.phase || this.Phase,
                round: roundKey
            });
            return original.apply(this, args);
        });

        // ===== 记录购买请求（参考0.1.0版本） =====
        wrapMethod(manager, 'ReqShopBuyChess', function (original, args) {
            const goodsID = args[0];
            const cost = typeof this.GetShopBuyCost === 'function' ? this.GetShopBuyCost(goodsID) : 0;
            const goods = typeof this.getShopGoodsByGoodsID === 'function' ? this.getShopGoodsByGoodsID(goodsID) : null;
            const roundKey = getRoundKey();
            // 直接记录购买
            const normalizedGoods = normalizeGoods(goods);
            latestBuyCard = normalizedGoods || latestBuyCard;
            recordCost('buy', {
                cost: Number(cost) || 0,
                goodsID: goodsID,
                goods: normalizedGoods,
                phase: this.phase || this.Phase,
                round: roundKey
            });
            return original.apply(this, args);
        });

        // ===== 记录商店快照 =====
        // 监听 updateShopGoods - 只记录 animate: true 的快照（第1回合除外）
        wrapMethod(manager, 'updateShopGoods', function (original, args) {
            const result = original.apply(this, args);
            const roundKey = getRoundKey();
            // 第1回合：全部保留；第2回合起：只保留 animate: true
            if (roundKey === '1') {
                recordShopGoods(args[0], { source: 'updateShopGoods', animate: args[1] });
            } else if (args[1] === true) {
                recordShopGoods(args[0], { source: 'updateShopGoods', animate: args[1] });
            }
            captureCardStates();
            return result;
        });

        // 刷新响应 - 记录快照
        wrapMethod(manager, 'onRespShopRefreshChess', function (original, args) {
            const result = original.apply(this, args);
            const proto = args[0] && args[0].Protocol && args[0].Protocol.ProtoData;
            if (proto && proto.shopGoods) {
                recordShopGoods(proto.shopGoods, { source: 'onRespShopRefreshChess', animate: true });
            }
            return result;
        });

        // ===== 记录锦囊使用 =====
        const managerCtor = (typeof window !== 'undefined' && window.TavernChessGameManager) || null;
        const spellHookTarget = managerCtor && managerCtor.prototype ? managerCtor.prototype : manager;

        wrapMethod(spellHookTarget, 'ReqChessUseSpell', function (original, args) {
            const spellGoodsID = args && (args[0] || 0);
            if (spellGoodsID) {
                const payload = resolveSpellUsePayload(this, spellGoodsID);
                if (payload) {
                    pendingSpellUses.push({
                        goodsID: spellGoodsID,
                        payload: payload,
                        round: getRoundKey()
                    });
                }
            }
            return original.apply(this, args);
        });

        wrapMethod(spellHookTarget, 'onRespChessUseSpell', function (original, args) {
            const proto = args[0] && args[0].Protocol && args[0].Protocol.ProtoData;
            const spellGoodsID = proto && (proto.spellGoodsID || proto.goodsID || proto.spellID || 0);
            const pending = spellGoodsID ? pendingSpellUses.filter(function (entry) {
                return entry.goodsID === spellGoodsID;
            }).pop() : null;
            const payload = pending && pending.payload ? pending.payload : resolveSpellUsePayload(this, spellGoodsID);

            const result = original.apply(this, args);
            if (!proto || proto.errCode) {
                pendingSpellUses = pendingSpellUses.filter(function (entry) {
                    return entry.goodsID !== spellGoodsID;
                });
                return result;
            }

            if (payload) {
                updateLatestSpellUse(payload, { source: 'onRespChessUseSpell', spellGoodsID: spellGoodsID, round: getRoundKey() });
            }
            pendingSpellUses = pendingSpellUses.filter(function (entry) {
                return entry.goodsID !== spellGoodsID;
            });
            captureCardStates();
            return result;
        });

        const spellEventName = (typeof window !== 'undefined' && window.TavernChessGameManager && window.TavernChessGameManager.CHESS_USE_SPELL)
            || 'CHESS_USE_SPELL';
        const respSpellEventName = (typeof window !== 'undefined' && window.TavernChessGameManager && window.TavernChessGameManager.RESP_CHESS_SPELL_USE)
            || 'RESP_CHESS_SPELL_USE';
        const teamSpellEventName = (typeof window !== 'undefined' && window.TavernChessGameManager && window.TavernChessGameManager.TEAM_SPELL_USE)
            || 'TEAM_SPELL_USE';

        if (typeof manager.on === 'function') {
            manager.on(spellEventName, manager, function (chessID, uniqueID, spellID) {
                const payload = buildSpellPayloadFromEvent(manager, spellID, chessID);
                if (payload) {
                    updateLatestSpellUse(payload, {
                        source: 'managerEvent:' + spellEventName,
                        spellID: spellID,
                        chessID: chessID,
                        round: getRoundKey()
                    });
                }
            });

            manager.on(respSpellEventName, manager, function (spellGoodsID) {
                if (!spellGoodsID) return;
                const payload = resolveSpellUsePayload(manager, spellGoodsID);
                if (payload) {
                    updateLatestSpellUse(payload, {
                        source: 'managerEvent:' + respSpellEventName,
                        spellGoodsID: spellGoodsID,
                        round: getRoundKey()
                    });
                }
            });

            manager.on(teamSpellEventName, manager, function (skillID, goodsID) {
                if (!skillID && !goodsID) return;
                const payload = buildSpellPayloadFromEvent(manager, skillID, goodsID);
                if (payload) {
                    latestSpellUseCard = payload;
                }
            });
        }

        const proxy = manager && manager.proxy;
        if (proxy && typeof proxy.SendProto === 'function' && !proxy[PROXY_SEND_HOOK_FLAG]) {
            const originalSendProto = proxy.SendProto;
            proxy.SendProto = function () {
                const argsList = Array.prototype.slice.call(arguments);
                const protoId = argsList[0];
                const protoData = argsList[1] || {};
                const spellGoodsID = protoId && protoData.spellGoodsID ? protoData.spellGoodsID : 0;
                if (spellGoodsID) {
                    const payload = resolveSpellUsePayload(manager, spellGoodsID);
                    pendingSpellUses.push({
                        goodsID: spellGoodsID,
                        payload: payload,
                        round: getRoundKey()
                    });
                }
                return originalSendProto.apply(this, argsList);
            };
            Object.defineProperty(proxy, PROXY_SEND_HOOK_FLAG, { value: true, configurable: true });
        }

        // 游戏结束自动导出
        ['onNotifyChessGameOver', 'onNotifyChessGameResult', 'onNtfChessGameOverMsg'].forEach(function (name) {
            wrapMethod(manager, name, function (original, args) {
                const result = original.apply(this, args);
                finalizeAndDownload(name);
                return result;
            });
        });

        updateStatus('统计中：' + ((currentStats && currentStats.tableId) || '未知房间'));
    }

    function attachSelfInfoHooks(manager) {
        const selfInfo = manager && (manager.selfInfo || manager.SelfInfo);
        if (!selfInfo || selfInfo[SELF_INFO_HOOK_FLAG]) return;
        Object.defineProperty(selfInfo, SELF_INFO_HOOK_FLAG, { value: true, configurable: true });
    }

    function resolveSpellUsePayload(manager, spellGoodsID) {
        if (!manager) return null;
        const candidates = [];
        const normalizedId = Number(spellGoodsID) || spellGoodsID || 0;

        if (typeof spellGoodsID === 'object' && spellGoodsID) {
            candidates.push(spellGoodsID);
        }

        if (typeof manager.GetHandChessByGoodID === 'function') {
            const fromManager = manager.GetHandChessByGoodID(normalizedId);
            if (fromManager) candidates.push(fromManager);
        }

        const selfInfo = manager.selfInfo || manager.SelfInfo || null;
        if (selfInfo && typeof selfInfo.GetChessByGoodID === 'function') {
            const fromSelfInfo = selfInfo.GetChessByGoodID(normalizedId);
            if (fromSelfInfo) candidates.push(fromSelfInfo);
        }

        const handChess = manager.HandChess || manager.handChess || (selfInfo && (selfInfo.HandChess || selfInfo.handChess)) || null;
        if (Array.isArray(handChess)) {
            const matched = handChess.filter(function (item) {
                return item && (item.goodsID === normalizedId || item.GoodsID === normalizedId || item.goodsID === String(normalizedId) || item.GoodsID === String(normalizedId));
            })[0];
            if (matched) candidates.push(matched);
        }

        const handSpellIDs = selfInfo && (selfInfo.handSpellIDs || selfInfo.handSpellIDs || selfInfo.chessSpellInfo || null);
        if (handSpellIDs && typeof handSpellIDs === 'object') {
            Object.keys(handSpellIDs).forEach(function (key) {
                const item = handSpellIDs[key];
                if (item && (item.goodsID === normalizedId || item.GoodsID === normalizedId || item.goodsID === String(normalizedId) || item.GoodsID === String(normalizedId))) {
                    candidates.push(item);
                }
            });
        }

        for (let i = 0; i < candidates.length; i++) {
            const handCard = candidates[i];
            if (!handCard || typeof handCard !== 'object') continue;
            const spellID = handCard.spellID || handCard.SpellID || 0;
            const chessID = handCard.chessID || handCard.ChessID || handCard.cardID || handCard.CardID || 0;
            const goodsID = handCard.goodsID || handCard.GoodsID || normalizedId || 0;
            const name = handCard.name || handCard.Name || handCard.cardName || handCard.CardName || '';
            if (spellID || chessID || goodsID) {
                return {
                    goodsID: goodsID,
                    spellID: spellID,
                    chessID: chessID,
                    name: name,
                    raw: handCard
                };
            }
        }

        return null;
    }

    function wrapMethod(target, name, wrapper) {
        if (!target || typeof target[name] !== 'function' || target[name].__tavernChessStatsWrapped) return;
        const original = target[name];
        const wrapped = function () {
            return wrapper.call(this, original, Array.prototype.slice.call(arguments));
        };
        wrapped.__tavernChessStatsWrapped = true;
        wrapped.__original = original;
        Object.defineProperty(target, name, { value: wrapped, configurable: true, writable: true });
    }

    function safeCall(obj, prop) {
        try { return obj[prop]; } catch (error) { return 0; }
    }

    function safeRead(obj, prop) {
        try { return obj[prop]; } catch (error) { return 0; }
    }

    // ===== 记录刷新/购买消耗 =====
    function recordCost(type, payload) {
        if (!currentStats) return;
        const cost = Number(payload && payload.cost) || 0;
        const roundKey = payload.round || getRoundKey();
        const roundStats = getRoundStats(roundKey);
        const target = roundStats[type];
        const total = currentStats.totals[type];

        target.count += 1;
        total.count += 1;
        target.hufu += cost;
        total.hufu += cost;

        if (type === 'refresh') {
            if (payload.isAuto) {
                target.autoCount += 1;
                total.autoCount += 1;
            }
            if (cost <= 0) {
                target.freeCount += 1;
                total.freeCount += 1;
            }
        }

        const entry = Object.assign({
            type: type,
            at: new Date().toISOString(),
            round: roundKey,
            phase: payload.phase || 0,
            cost: cost
        }, payload);

        target.details.push(entry);
        total.details.push(entry);

        if (target.details.length > 50) target.details = target.details.slice(-50);
        if (total.details.length > 50) total.details = total.details.slice(-50);

        currentStats.events.push({ type, at: new Date().toISOString(), round: roundKey, payload: entry });
        trimEvents();
        saveStats();
        renderPanel();
    }

    // ===== 记录商店快照 =====
    function recordShopGoods(shopGoods, context) {
        if (!currentStats || !Array.isArray(shopGoods)) return;

        const roundKey = getRoundKey();

        // 过滤规则：
        // 1. 第1回合：全部保留
        // 2. 第2回合起：只保留 animate: true
        if (roundKey !== '1' && context.animate !== true) {
            return;
        }

        const goodsList = shopGoods.map(function (goods, index) {
            const normalized = normalizeGoods(goods);
            if (normalized) normalized.slotIndex = index;
            return normalized;
        }).filter(Boolean);
        recordCardStateSnapshot('shop', goodsList, { source: context.source || 'shop', round: roundKey });
        if (goodsList.length === 0) return;

        // 去重：检查是否与上一次记录相同
        const signature = roundKey + ':' + goodsList.map(function (goods) {
            return (goods.goodsID || 0) + ':' + (goods.chessID || 0) + ':' + (goods.spellID || 0);
        }).join('|');
        if (signature === lastShopSignature) return;
        lastShopSignature = signature;

        const now = new Date().toISOString();
        const roundStats = getRoundStats(roundKey);
        const snapshot = { at: now, round: roundKey, context: context || {}, goods: goodsList };

        roundStats.shop.snapshots += 1;
        currentStats.totals.shop.snapshots += 1;
        roundStats.shop.details.push(snapshot);
        currentStats.totals.shop.details.push(snapshot);

        if (roundStats.shop.details.length > 50) roundStats.shop.details = roundStats.shop.details.slice(-50);
        if (currentStats.totals.shop.details.length > 50) currentStats.totals.shop.details = currentStats.totals.shop.details.slice(-50);

        goodsList.forEach(function (goods) {
            addAppearance(roundStats.shop.appearances, goods);
            addAppearance(currentStats.totals.shop.appearances, goods);
            addAppearance(currentStats.shopAppearances, goods);
        });

        currentStats.events.push({ type: 'shop', at: now, round: roundKey, context: context || {}, count: goodsList.length });
        trimEvents();
        saveStats();
        renderPanel();
    }

    function addAppearance(bucket, goods) {
        const key = String(goods.spellID ? 'spell:' + goods.spellID : goods.chessID || goods.cardID || goods.goodsID || goods.name || 'unknown');
        if (!bucket[key]) bucket[key] = { count: 0, sample: goods, times: [] };
        bucket[key].count += 1;
        bucket[key].times.push(new Date().toISOString());
    }

    function recordCardStateSnapshot(area, cards, context) {
        if (!currentStats || !Array.isArray(cards)) return;
        const normalized = cards.map(function (card) {
            const item = normalizeGoods(card);
            if (!item) return null;
            return Object.assign({}, item, {
                area: area,
                at: new Date().toISOString(),
                round: getRoundKey()
            });
        }).filter(Boolean);
        if (!normalized.length) return;

        currentStats.cardStates[area] = normalized;
        const roundStats = getRoundStats(getRoundKey());
        roundStats.cardStates[area] = normalized;
        currentStats.snapshots.push(Object.assign({
            at: new Date().toISOString(),
            round: getRoundKey(),
            area: area,
            cards: normalized
        }, context || {}));
        if (currentStats.snapshots.length > 200) currentStats.snapshots = currentStats.snapshots.slice(-200);
        saveStats();
        renderPanel();
    }

    function captureCardStates() {
        if (!currentManager) return;
        const manager = currentManager;
        const handCards = Array.isArray(manager.HandChess || manager.handChess)
            ? (manager.HandChess || manager.handChess)
            : [];
        const lineupCards = Array.isArray(manager.selfInfo && (manager.selfInfo.LineUpChess || manager.selfInfo.lineUpChess))
            ? (manager.selfInfo.LineUpChess || manager.selfInfo.lineUpChess)
            : [];

        if (lineupCards && lineupCards.length) {
            latestLineupCard = normalizeGoods(lineupCards[lineupCards.length - 1]) || latestLineupCard;
        }

        recordCardStateSnapshot('hand', handCards, { source: 'captureCardStates', round: getRoundKey() });
        recordCardStateSnapshot('lineup', lineupCards, { source: 'captureCardStates', round: getRoundKey() });
    }

    function normalizeGoods(goods) {
        if (!goods || typeof goods !== 'object') return null;
        const chessID = goods.chessID || goods.cardID || goods.CardID || 0;
        const spellID = goods.spellID || goods.SpellID || 0;
        const cardID = chessID || spellID || goods.itemID || goods.ItemID || 0;

        let name = '';
        if (spellID > 0) {
            name = getSpellName(spellID);
        } else if (chessID > 0) {
            name = getChessName(chessID);
        }
        if (!name || name === String(chessID) || name === String(spellID)) {
            name = goods.name || goods.Name || goods.cardName || goods.CardName || '';
        }
        if (!name || name === 'unknown' || name === '') {
            try {
                if (cardID && window.TavernChessConfiger) {
                    const card = TavernChessConfiger.GetInstance().GetCardByCardID(cardID);
                    name = card && (card.CardName || card.Name || card.name || card.SpellName || '') || '';
                }
            } catch (error) {}
        }
        if (!name || name === 'unknown' || name === '') {
            name = String(cardID || goods.goodsID || 'unknown');
        }

        return {
            goodsID: goods.goodsID || goods.GoodsID || goods.id || 0,
            chessID: chessID,
            spellID: spellID,
            cardID: cardID,
            name: name,
            rank: goods.rank || goods.Rank || goods.ChessRank || 0,
            rawPosition: goods.position || goods.Position || 0
        };
    }

    function recordSpell(spellLike, context) {
        if (!currentStats || !spellLike || typeof spellLike !== 'object') return;
        const normalized = normalizeGoods(spellLike);
        const spellKey = normalized && (normalized.spellID || normalized.chessID || normalized.cardID || normalized.goodsID || 0);
        if (!normalized || !spellKey) return;

        const dedupeKey = [getRoundKey(), normalized.spellID || 0, normalized.chessID || 0, normalized.goodsID || 0, normalized.cardID || 0].join(':');
        const now = Date.now();
        for (const [key, ts] of recordedSpellInstanceKeys.entries()) {
            if (now - ts > SPELL_DUPE_WINDOW_MS) {
                recordedSpellInstanceKeys.delete(key);
            }
        }
        if (recordedSpellInstanceKeys.has(dedupeKey)) return;
        recordedSpellInstanceKeys.set(dedupeKey, now);

        const key = String(spellKey);
        if (!currentStats.spells[key]) {
            currentStats.spells[key] = { count: 0, sample: normalized, times: [] };
        }
        currentStats.spells[key].count += 1;
        currentStats.spells[key].times.push(new Date().toISOString());

        currentStats.events.push({ type: 'spell', at: new Date().toISOString(), payload: { context: context || {}, spell: normalized } });
        trimEvents();
        saveStats();
        renderPanel();
    }

    function updateLatestSpellUse(payload, context) {
        if (!payload || typeof payload !== 'object') return;
        latestSpellUseCard = payload;
        if (payload.spellID || payload.chessID || payload.cardID || payload.goodsID) {
            const normalized = normalizeGoods(payload);
            if (normalized) {
                const dedupeKey = [getRoundKey(), normalized.spellID || 0, normalized.chessID || 0, normalized.goodsID || 0, normalized.cardID || 0].join(':');
                const now = Date.now();
                for (const [key, ts] of recordedSpellInstanceKeys.entries()) {
                    if (now - ts > SPELL_DUPE_WINDOW_MS) {
                        recordedSpellInstanceKeys.delete(key);
                    }
                }
                if (!recordedSpellInstanceKeys.has(dedupeKey)) {
                    recordedSpellInstanceKeys.set(dedupeKey, now);
                    recordSpell(normalized, context);
                }
            }
        }
    }

    function trimEvents() {
        if (currentStats.events.length > 600) currentStats.events.splice(0, currentStats.events.length - 600);
        currentStats.updatedAt = new Date().toISOString();
    }

    function saveStats() {
        if (!currentStats) return;
        localStorage.setItem(STORAGE_KEY, JSON.stringify(currentStats));
        localStorage.setItem(STORAGE_KEY + ':' + currentStats.gameId, JSON.stringify(currentStats));
    }

    function createPanel() {
        if (document.getElementById(PANEL_ID)) return;
        const state = loadPanelState();
        const panel = document.createElement('div');
        panel.id = PANEL_ID;
        panel.style.cssText = [
            'position:fixed',
            'left:' + state.left + 'px',
            'top:' + state.top + 'px',
            'z-index:99999',
            'width:' + state.width + 'px',
            'height:' + state.height + 'px',
            'min-width:220px',
            'min-height:150px',
            'max-width:80vw',
            'max-height:80vh',
            'overflow:hidden',
            'resize:both',
            'background:rgba(20,24,35,.92)',
            'color:#f6f7fb',
            'border:1px solid #596275',
            'border-radius:10px',
            'font:12px/1.45 Microsoft YaHei,Arial,sans-serif',
            'box-shadow:0 6px 22px rgba(0,0,0,.35)'
        ].join(';');
        panel.innerHTML = '<div id="tavern-chess-stats-header" style="display:flex;justify-content:space-between;align-items:center;gap:6px;padding:8px 10px;cursor:move;background:rgba(75,123,236,.18);user-select:none;"><b>自走棋统计</b><div style="display:flex;gap:4px;"><button id="tavern-chess-stats-minimize" style="cursor:pointer;border:0;border-radius:6px;padding:4px 8px;background:#596275;color:#fff;">最小化</button><button id="tavern-chess-stats-export" style="cursor:pointer;border:0;border-radius:6px;padding:4px 8px;background:#4b7bec;color:#fff;">导出</button><button id="tavern-chess-stats-hide" style="cursor:pointer;border:0;border-radius:6px;padding:4px 8px;background:#596275;color:#fff;">收起</button></div></div><div id="tavern-chess-stats-content" style="height:calc(100% - 42px);overflow:auto;padding:8px 10px;"><div id="tavern-chess-stats-status" style="color:#a5b1c2;margin-bottom:6px;">初始化中...</div><div id="tavern-chess-stats-body"></div></div>';
        document.body.appendChild(panel);
        panelContent = document.getElementById('tavern-chess-stats-content');
        panelBody = document.getElementById('tavern-chess-stats-body');
        panelStatus = document.getElementById('tavern-chess-stats-status');

        document.getElementById('tavern-chess-stats-export').addEventListener('click', function () { exportStats(false); });
        document.getElementById('tavern-chess-stats-hide').addEventListener('click', togglePanelContent);
        document.getElementById('tavern-chess-stats-minimize').addEventListener('click', toggleMinimize);

        makePanelDraggable(panel, document.getElementById('tavern-chess-stats-header'));
        observePanelResize(panel);
    }

    function toggleMinimize() {
        const panel = document.getElementById(PANEL_ID);
        if (!panel) return;
        const content = document.getElementById('tavern-chess-stats-content');
        const header = document.getElementById('tavern-chess-stats-header');
        const button = document.getElementById('tavern-chess-stats-minimize');

        if (isMinimized) {
            content.style.display = 'block';
            header.querySelector('b').textContent = '自走棋统计';
            panel.style.width = '300px';
            panel.style.height = '420px';
            panel.style.minWidth = '220px';
            panel.style.minHeight = '150px';
            button.textContent = '最小化';
            isMinimized = false;
            setTimeout(function() { savePanelState(panel); }, 100);
        } else {
            content.style.display = 'none';
            header.querySelector('b').textContent = '📊 统计';
            panel.style.width = 'auto';
            panel.style.height = 'auto';
            panel.style.minWidth = 'auto';
            panel.style.minHeight = 'auto';
            button.textContent = '恢复';
            isMinimized = true;
        }
    }

    function loadPanelState() {
        const fallback = { left: Math.max(12, window.innerWidth - 320), top: Math.max(12, window.innerHeight - 470), width: 300, height: 420 };
        try {
            return Object.assign(fallback, JSON.parse(localStorage.getItem(PANEL_STATE_KEY) || '{}'));
        } catch (error) {
            return fallback;
        }
    }

    function savePanelState(panel) {
        if (isMinimized) return;
        const rect = panel.getBoundingClientRect();
        localStorage.setItem(PANEL_STATE_KEY, JSON.stringify({ left: Math.round(rect.left), top: Math.round(rect.top), width: Math.round(rect.width), height: Math.round(rect.height) }));
    }

    function makePanelDraggable(panel, header) {
        let dragging = false;
        let startX = 0, startY = 0, startLeft = 0, startTop = 0;
        header.addEventListener('mousedown', function (event) {
            if (event.target && event.target.tagName === 'BUTTON') return;
            dragging = true;
            startX = event.clientX;
            startY = event.clientY;
            const rect = panel.getBoundingClientRect();
            startLeft = rect.left;
            startTop = rect.top;
            event.preventDefault();
        });
        document.addEventListener('mousemove', function (event) {
            if (!dragging) return;
            const nextLeft = Math.min(window.innerWidth - 60, Math.max(0, startLeft + event.clientX - startX));
            const nextTop = Math.min(window.innerHeight - 40, Math.max(0, startTop + event.clientY - startY));
            panel.style.left = nextLeft + 'px';
            panel.style.top = nextTop + 'px';
            panel.style.right = 'auto';
            panel.style.bottom = 'auto';
        });
        document.addEventListener('mouseup', function () {
            if (!dragging) return;
            dragging = false;
            savePanelState(panel);
        });
    }

    function observePanelResize(panel) {
        if (window.ResizeObserver) {
            const observer = new ResizeObserver(function () { savePanelState(panel); });
            observer.observe(panel);
        }
    }

    function togglePanelContent() {
        const content = document.getElementById('tavern-chess-stats-content');
        const button = document.getElementById('tavern-chess-stats-hide');
        if (!content || !button) return;
        if (isMinimized) return;
        const hide = content.style.display !== 'none';
        content.style.display = hide ? 'none' : 'block';
        button.textContent = hide ? '展开' : '收起';
    }

    function updateStatus(text) {
        if (panelStatus) panelStatus.textContent = text;
    }

    function renderPanel() {
        if (!panelBody) return;
        if (!currentStats) {
            panelBody.innerHTML = '<div>尚未进入自走棋对局。</div>';
            return;
        }
        const total = currentStats.totals;
        const roundKey = getRoundKey();
        const roundStats = currentStats.rounds[roundKey] || createRoundStats('当前回合');
        const topCards = getTopEntries(currentStats.shopAppearances, 8);
        const topSpells = getTopEntries(currentStats.spells, 6);
        const handCards = (currentStats.cardStates && currentStats.cardStates.hand || []).slice(0, 12);
        const lineupCards = (currentStats.cardStates && currentStats.cardStates.lineup || []).slice(0, 12);
        const shopCards = (currentStats.cardStates && currentStats.cardStates.shop || []).slice(0, 12);
        panelBody.innerHTML = [
            '<b style="display:block;margin-top:6px;">最近操作</b>',
            '<div><b>购买</b></div>' + renderCardItem(latestBuyCard),
            '<div><b>上阵</b></div>' + renderCardItem(latestLineupCard),
            '<div><b>使用锦囊</b></div>' + renderCardItem(latestSpellUseCard),
            '<div>房间：' + escapeHtml(currentStats.tableId || '未知') + '</div>',
            '<div>当前：第 ' + escapeHtml(roundKey) + ' 回合</div>',
            '<hr style="border:0;border-top:1px solid rgba(255,255,255,.12);margin:6px 0;">',
            '<b>整局</b>',
            '<div>刷新：' + total.refresh.count + ' 次 / ' + total.refresh.hufu + ' 虎符（免费 ' + total.refresh.freeCount + '，自动 ' + total.refresh.autoCount + '）</div>',
            '<div>购买：' + total.buy.count + ' 次 / ' + total.buy.hufu + ' 虎符</div>',
            '<div>商店快照：' + total.shop.snapshots + ' 次</div>',
            '<b style="display:block;margin-top:6px;">本回合</b>',
            '<div>刷新：' + roundStats.refresh.count + ' 次 / ' + roundStats.refresh.hufu + ' 虎符</div>',
            '<div>购买：' + roundStats.buy.count + ' 次 / ' + roundStats.buy.hufu + ' 虎符</div>',
            '<div>商店快照：' + roundStats.shop.snapshots + ' 次</div>',
            '<b style="display:block;margin-top:6px;">区域卡牌</b>',
            '<div><b>手牌区</b></div>' + renderCardList(handCards),
            '<div><b>上阵区</b></div>' + renderCardList(lineupCards),
            '<div><b>商店区</b></div>' + renderCardList(shopCards),
            '<b style="display:block;margin-top:6px;">本局锦囊</b>',
            renderEntryList(topSpells),
            '<b style="display:block;margin-top:6px;">卡牌出现 Top</b>',
            renderEntryList(topCards)
        ].join('');
    }

    function getTopEntries(bucket, limit) {
        return Object.keys(bucket || {}).map(function (key) {
            const item = bucket[key];
            return { key, count: item.count, name: item.sample && item.sample.name };
        }).sort(function (a, b) { return b.count - a.count; }).slice(0, limit);
    }

    function renderEntryList(entries) {
        if (!entries.length) return '<div style="color:#a5b1c2;">暂无数据</div>';
        return entries.map(function (item) {
            return '<div style="display:flex;justify-content:space-between;border-bottom:1px solid rgba(255,255,255,.08);"><span>' + escapeHtml(item.name || item.key) + '</span><b>' + item.count + '</b></div>';
        }).join('');
    }

    function renderCardList(cards) {
        if (!cards || !cards.length) return '<div style="color:#a5b1c2;">暂无数据</div>';
        return cards.map(function (item) {
            return '<div style="display:flex;justify-content:space-between;border-bottom:1px solid rgba(255,255,255,.08);"><span>' + escapeHtml(item.name || item.key || (item.spellID ? ('锦囊:' + item.spellID) : (item.chessID || item.cardID || item.goodsID || '未知'))) + '</span><b>' + escapeHtml(item.goodsID || item.cardID || '') + '</b></div>';
        }).join('');
    }

    function renderCardItem(card) {
        if (!card) return '<div style="color:#a5b1c2;">暂无数据</div>';
        const label = card.name || (card.spellID ? ('锦囊:' + card.spellID) : (card.chessID || card.cardID || card.goodsID || '未知'));
        const goodsId = card.goodsID || card.cardID || '';
        return '<div style="display:flex;justify-content:space-between;border-bottom:1px solid rgba(255,255,255,.08);"><span>' + escapeHtml(label) + '</span><b>' + escapeHtml(goodsId) + '</b></div>';
    }

    function finalizeAndDownload(reason) {
        if (!currentStats || downloadedGameIds.has(currentStats.gameId)) return;
        downloadedGameIds.add(currentStats.gameId);
        currentStats.endedAt = new Date().toISOString();
        currentStats.endReason = reason;
        saveStats();
        exportStats(true);
        updateStatus('本局结束，已打包下载统计结果');
    }

    function exportStats(forceDownload) {
        if (!currentStats) return;
        const text = JSON.stringify(currentStats, null, 2);
        if (forceDownload) {
            downloadStats(text);
            return;
        }
        if (navigator.clipboard) {
            navigator.clipboard.writeText(text).then(function () {
                updateStatus('已复制 JSON 到剪贴板');
            }).catch(function () {
                downloadStats(text);
            });
        } else {
            downloadStats(text);
        }
    }

    function downloadStats(text) {
        const blob = new Blob([text], { type: 'application/json' });
        const url = URL.createObjectURL(blob);
        const link = document.createElement('a');
        link.href = url;
        link.download = 'tavern-chess-stats-' + ((currentStats && currentStats.gameId) || Date.now()) + '.json';
        document.body.appendChild(link);
        link.click();
        link.remove();
        URL.revokeObjectURL(url);
        updateStatus('已下载 JSON');
    }

    function escapeHtml(value) {
        return String(value).replace(/[&<>"]/g, function (char) {
            return ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;' })[char];
        });
    }

    waitAndAttach();
    console.log(LOG_PREFIX, '脚本 v0.3.0 已启动');
})();